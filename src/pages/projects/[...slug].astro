---
import { getCollection } from 'astro:content';
import ArticleLayout from '@layouts/ArticleLayout.astro';

export async function getStaticPaths() {
  const projects = await getCollection('projects');
  return projects.map(project => ({
    params: { slug: project.slug },
    props: { project },
  }));
}

const { project } = Astro.props;
const { Content } = await project.render();

const statusConfig = {
  'active': { label: 'Active Development', color: 'text-green-500', bg: 'bg-green-950', border: 'border-green-900' },
  'archived': { label: 'Archived', color: 'text-gray-500', bg: 'bg-gray-950', border: 'border-gray-900' },
  'maintenance': { label: 'Maintenance Mode', color: 'text-yellow-500', bg: 'bg-yellow-950', border: 'border-yellow-900' }
};

const status = statusConfig[project.data.status];

function parseGitHub(url: string) {
  const match = url.match(/github\.com\/([^\/]+)\/([^\/]+)/);
  if (!match) return null;
  return { owner: match[1], repo: match[2].replace('.git', '') };
}

const github = parseGitHub(project.data.repository);
---

<ArticleLayout 
  title={`${project.data.title} | Genix`}
  description={project.data.description}
  backLink="/projects"
  backText="back to projects">
  
  <article 
    class="space-y-10"
    data-github-owner={github?.owner}
    data-github-repo={github?.repo}>
    <header class="space-y-6 pb-8 border-b border-gray-800">
      <div class="space-y-4">
        <div class="flex items-start justify-between gap-4">
          <h1 class="text-3xl md:text-4xl lg:text-5xl text-white font-bold leading-tight tracking-tight flex-1">
            {project.data.title}
          </h1>
          <div class={`text-sm px-4 py-2 rounded border ${status.bg} ${status.color} ${status.border} whitespace-nowrap`}>
            {status.label}
          </div>
        </div>
        
        <p class="text-lg text-gray-200 leading-relaxed">
          {project.data.description}
        </p>
      </div>

      <div class="flex flex-wrap gap-2">
        {project.data.tags.map(tag => (
          <span class="text-sm text-gray-300 bg-gray-950 px-3 py-1 rounded border border-gray-900">
            {tag}
          </span>
        ))}
      </div>

      <div class="flex flex-wrap items-center gap-4 text-base">
        {github && (
          <div class="flex items-center gap-4 text-gray-400">
            <span class="github-stars" data-loading="true">⭐ ...</span>
            <span class="github-forks" data-loading="true">🍴 ...</span>
          </div>
        )}
        {project.data.repository && (
          <a href={project.data.repository}
             target="_blank"
             rel="noopener noreferrer"
             class="text-gray-300 hover:text-white transition-colors">
            → View Source
          </a>
        )}
      </div>

      {project.data.image && (
        <div class="mt-6">
          <img
            src={project.data.image}
            alt={project.data.title}
            class="w-full h-auto rounded-lg border border-gray-800"
            loading="lazy"
          />
        </div>
      )}
    </header>

    <div class="article-content prose prose-invert prose-lg max-w-none">
      <Content />
    </div>

    <footer class="pt-8 border-t border-gray-900">
      <a href="/projects" class="text-gray-300 hover:text-white transition-colors text-base">
        ← All projects
      </a>
    </footer>
  </article>
</ArticleLayout>

<script>
  document.addEventListener('DOMContentLoaded', async () => {
    const article = document.querySelector('article[data-github-owner]');
    if (!article) return;
    
    const owner = article.getAttribute('data-github-owner');
    const repo = article.getAttribute('data-github-repo');
    
    if (!owner || !repo) return;
    
    try {
      const response = await fetch(`https://api.github.com/repos/${owner}/${repo}`);
      if (!response.ok) return;
      
      const data = await response.json();
      
      const starsEl = article.querySelector('.github-stars');
      const forksEl = article.querySelector('.github-forks');
      
      if (starsEl) {
        starsEl.textContent = `⭐ ${data.stargazers_count.toLocaleString()}`;
        starsEl.removeAttribute('data-loading');
      }
      
      if (forksEl) {
        forksEl.textContent = `🍴 ${data.forks_count.toLocaleString()}`;
        forksEl.removeAttribute('data-loading');
      }
    } catch (error) {
      console.error('Failed to fetch GitHub stats:', error);
    }
  });
</script>

<style>
  .article-content {
    color: #e5e5e5;
    line-height: 1.8;
  }
  
  .article-content :global(h1),
  .article-content :global(h2),
  .article-content :global(h3),
  .article-content :global(h4) {
    color: #ffffff;
    font-weight: 700;
    margin-top: 2.5rem;
    margin-bottom: 1rem;
  }
  
  .article-content :global(h1) { font-size: 2.25rem; line-height: 1.2; }
  .article-content :global(h2) { font-size: 2rem; line-height: 1.3; }
  .article-content :global(h3) { font-size: 1.625rem; line-height: 1.4; }
  .article-content :global(h4) { font-size: 1.375rem; line-height: 1.5; }
  
  .article-content :global(p) {
    margin-top: 1.25rem;
    margin-bottom: 1.25rem;
    color: #e0e0e0;
    font-size: 1.0625rem;
  }
  
  .article-content :global(a) {
    color: #b0b0b0;
    text-decoration: underline;
    text-underline-offset: 3px;
    transition: color 0.2s;
  }
  
  .article-content :global(a:hover) { color: #ffffff; }
  
  .article-content :global(code) {
    color: #f0f0f0;
    background: rgba(255, 255, 255, 0.08);
    padding: 0.25rem 0.5rem;
    border-radius: 0.25rem;
    font-size: 0.9em;
  }
  
  .article-content :global(pre) {
    background: rgba(0, 0, 0, 0.6);
    border: 1px solid rgba(255, 255, 255, 0.15);
    border-radius: 0.5rem;
    padding: 1.5rem;
    overflow-x: auto;
    margin: 2rem 0;
  }
  
  .article-content :global(pre code) {
    background: transparent;
    padding: 0;
    color: #e5e5e5;
  }
  
  .article-content :global(blockquote) {
    border-left: 4px solid #666;
    padding-left: 1.5rem;
    color: #b8b8b8;
    font-style: italic;
    margin: 2rem 0;
  }
  
  .article-content :global(ul),
  .article-content :global(ol) {
    color: #e0e0e0;
    padding-left: 1.75rem;
    margin: 1.25rem 0;
  }
  
  .article-content :global(li) { margin: 0.625rem 0; }
  .article-content :global(strong) { color: #ffffff; font-weight: 600; }
  .article-content :global(em) { color: #f0f0f0; font-style: italic; }
  .article-content :global(hr) { border-color: #444; margin: 3rem 0; }
  
  .article-content :global(img) {
    border-radius: 0.5rem;
    border: 1px solid rgba(255, 255, 255, 0.15);
    margin: 2rem 0;
  }

  .article-content :global(table) {
    width: 100%;
    border-collapse: collapse;
    margin: 2rem 0;
  }

  .article-content :global(th),
  .article-content :global(td) {
    border: 1px solid rgba(255, 255, 255, 0.15);
    padding: 0.75rem;
    text-align: left;
    color: #e0e0e0;
  }

  .article-content :global(th) {
    background: rgba(255, 255, 255, 0.08);
    font-weight: 600;
    color: #ffffff;
  }
  
  [data-loading="true"] {
    opacity: 0.5;
  }
</style>